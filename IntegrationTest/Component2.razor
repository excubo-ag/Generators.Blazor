@foreach (var element in Enumerable.Empty<string>())
{
    <div class="my-component">
        This Blazor component is defined in the <strong>IntegrationTest</strong> package. @element
    </div>
}